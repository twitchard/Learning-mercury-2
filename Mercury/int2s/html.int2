:- module html.
:- interface.
:- type attribute
    --->    attribute(string, string).
:- type html
    --->    empty 
    ;       text(string)
    ;       element(html.tag, html.is_void, list.list(html.attribute), list.list(html.html))
    ;       concat(list.list(html.html)).
:- type is_void
    --->    yes 
    ;       no .
:- type tag == string.
